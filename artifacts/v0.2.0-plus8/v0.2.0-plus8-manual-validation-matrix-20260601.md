# v0.2.0+8 Manual Validation Matrix

- **Created at:** 2026-06-01
- **Runtime target:** `https://guarappari.belluga.space` served from `web-app`
- **Web bundle provenance:** `web-app/index.html` exposes `window.__WEB_BUILD_SHA__ = "04ba7216"`
- **Tenant admin entry:** `https://guarappari.belluga.space/admin`
- **Reference public profile:** `https://guarappari.belluga.space/parceiro/qa-discovery-tag-longa`
- **Reference public event:** `https://guarappari.belluga.space/agenda/evento/pw-event-share-boundary-store-release`
- **Admin credential:** use the Guarapari tenant-admin credential agreed in the local validation thread.

## Automated Gate Status

| Gate | Command | Result | Notes |
| --- | --- | --- | --- |
| Flutter Event cold-start route parity | `cd flutter-app && fvm flutter test test/application/startup/app_startup_navigation_coordinator_test.dart test/platform/deep_link_platform_config_test.dart` | `passed` | 14 tests. Adds `/agenda/evento/:slug?occurrence=...` parity beside `/parceiro/:slug`. |
| Laravel web-to-app deep-link contract | `./scripts/delphi/run_laravel_tests_safe.sh tests/Api/v1/Tenants/Branding/ApiV1OpenAppRedirectTest.php tests/Api/v1/Tenants/Branding/ApiV1DeferredDeepLinkResolverTest.php` | `passed` | 13 tests / 169 assertions. Event target and query are preserved through Android/deferred handoff. |
| Browser readonly matrix | `source ~/.nvm/nvm.sh && nvm use 24.13.1 >/dev/null && bash tools/flutter/run_web_navigation_smoke.sh readonly` | `passed` | 16/16. Runner is serialized by default for public-tunnel determinism; prior two-worker attempts failed with Cloudflare 502/Host Error, not product assertions. |
| Browser mutation matrix | `source ~/.nvm/nvm.sh && nvm use 24.13.1 >/dev/null && NAV_ADMIN_PASSWORD='<tenant-admin password>' bash tools/flutter/run_web_navigation_smoke.sh mutation` | `passed` | 26/26. Covers admin/public filters, map override/no-override, route chooser, reference point modal, invite boundary, nested/admin/media/occurrence flows. |
| Android installed-app cold start | `adb devices -l` plus ADB cold-start smoke | `blocked` | No attached ADB device in WSL. This remains the only unclosed native runtime proof for the fast-follow Event cold-start TODO. |

## Manual Matrix

| TODO | Admin Validation | Public Validation | Expected Result | Automated Coverage |
| --- | --- | --- | --- | --- |
| `cover-crop-560x512` | Open an Account Profile edit screen, add/replace cover, open crop sheet, confirm crop, save/reload. Also check avatar upload separately. | Open the public profile after reload and inspect cover framing. | Cover crop uses the new wider ratio without affecting avatar or other image slots; persisted cover remains after reload. | Focused Flutter crop/ingestion tests; mutation `tenant-admin account-profile cover upload persists and renders after reload`; bundle `04ba7216`. |
| `event-directions-inline-provider-actions` | No admin setup required beyond an event/profile with coordinates. | Open the reference event and its `Como Chegar` tab. Validate inline `Waze`, `Uber`, and `Outros`; open `Outros` and verify compact branded rows for Google Maps, Waze, Uber, and 99. | Direct provider buttons do not open the chooser; `Outros` opens the shared compact chooser; no duplicated Google row; labels/logos are branded. | Widget/service tests for `DirectionsAppChooser`; browser specs; mutation `NAV-APD-09 Como Chegar opens focused map and shared route chooser`. |
| `immersive-hero-actions-centralization` | No admin setup required. | Open event, account profile, and static asset immersive pages. Expanded hero shows vertical action rail; collapsed hero shows primary action plus more. On Event, `Convidar` opens invite route/promotion boundary; share opens share sheet; WhatsApp attempts native app handoff. On Account/Profile, favorite toggles in place. | Same centralized hero action model across Event/Account/Static; Event hero height is 65%; Account hero height is 50%; no divider/shadow line before tabs while expanded. | Focused Flutter hero suite; analyzer/build gates; readonly browser smoke against build `04ba7216`; mutation `T6-EVENT-SHARE anonymous web event invite preserves promotion boundary`. |
| `map-filter-event-types-catalog-hydration` | Go to `/admin/settings`, open `Filtros`, then `Mapa`. Open/edit primary map filters and rule catalog. | Open `/mapa`, dismiss location prompt, open filter control. | Map filters use backend-configured labels/query values and do not leak Home/Discovery taxonomy-only rows as baseline map filters. | Focused admin filter tests; mutation `tenant-admin keeps public Map filter config in the canonical filters editor`; mutation `public Map keeps baseline primary filters without taxonomy subfilters and uses backend filtering`. |
| `map-filter-visual-override-decoupling` | In `/admin/filters/surface?surface=public_map.primary`, configure two filters: one with marker override enabled and one with visual set but marker override disabled. Reopen editor. | Open `/mapa`, compare filter buttons/chips and selected POI markers for both filters. | Button/chip visual always uses saved backend visual; marker/item visual changes only when `override_marker=true`; label, color, icon/logo come from backend config. | Focused Laravel/Flutter tests; mutation `public Map navigates configured canonical filters with and without marker override visuals`. |
| `nested-account-profile-groups` | Open Account Profile edit, create at least two nested groups with different labels and selected linked Account Profiles; reorder one group/member; save/reopen. | Open the parent public profile and verify one tab per non-empty group; click a linked profile card. | Non-empty groups render as custom public tabs in persisted order; empty groups stay hidden; linked cards use Account Profile visuals and navigate to `/parceiro/:slug`. | Laravel nested feature/projection tests; Flutter DTO/admin/public tests; v0.2.0+8 consolidated CI-equivalent artifacts. |
| `reference-poi-reference-point-actions` | Ensure the profile type has `is_reference_location_enabled=true` for the tested Account Profile type. | Open the reference Account Profile. Click `Usar como ponto de referência`; confirm modal text `Todas as distâncias serão calculadas a partir desse local:` and reduced profile card; confirm. Repeat from `/mapa` POI card. | The action uses the location icon, opens confirmation modal before persisting, saves user-owned fixed reference, selected state changes to current reference, and future distance/route prompts use that reference. | Flutter repository/domain tests; map card and Account Profile hero tests; mutation `NAV-APD-11 reference point action opens confirmation modal`; mutation `NAV-APD-13 map reference point action opens confirmation modal`. |
| Fast-follow `event-cold-start-deep-link-routing` | Not admin-driven. Confirm app package/build under test. | On Android device with Guarapari app installed: force-stop app, open `https://guarappari.com.br/agenda/evento/show-rock?occurrence=occ-1`; repeat with `/parceiro/profile-slug`; repeat Event while app is warm. | Event cold start must open Event detail and preserve `occurrence`, matching Account Profile cold-start behavior. Warm Event remains green. | Source/backend/router/manifest evidence is green; native ADB smoke is blocked locally because no device is attached. |

## Native Android Cold-Start Smoke To Run When Device Is Attached

```bash
cd flutter-app
adb devices -l
adb -s <serial> shell am force-stop com.guarappari.app
adb -s <serial> shell am start -W \
  -a android.intent.action.VIEW \
  -c android.intent.category.BROWSABLE \
  -d 'https://guarappari.com.br/agenda/evento/show-rock?occurrence=occ-1' \
  com.guarappari.app
adb -s <serial> shell am force-stop com.guarappari.app
adb -s <serial> shell am start -W \
  -a android.intent.action.VIEW \
  -c android.intent.category.BROWSABLE \
  -d 'https://guarappari.com.br/parceiro/profile-slug' \
  com.guarappari.app
```

If this fails only on `guarappari.belluga.space` or `guarappari.belluga.app`, the current host-association evidence already shows those preview hosts return an empty `assetlinks.json`; production `guarappari.com.br` and `guarappari.booraagora.com.br` return the Guarapari Android app association.
